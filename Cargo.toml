[workspace]
members = ["node", "devnet", "libs/chain-signatures/contract", "libs/chain-signatures/keys"]
resolver = "2"
exclude = ["libs", "pytest"]

[profile.dev-release]
inherits = "release"
debug = true
