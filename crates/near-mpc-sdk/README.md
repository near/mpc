# near-mpc-sdk

Client SDK for the MPC contract: request builders, response verification,
and validation shared between the contract and its clients.

Logic that must agree between the contract and its consumers — most
importantly validation of requests and responses — belongs here.
